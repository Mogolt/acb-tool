# Screenshots

TODO: add Browse / Extract / Inject / Convert tab screenshots here and
link them from the top-level `README.md`.

Suggested shots (900x640 window each):
- `browse.png`   — ACB loaded, cue tree + waveform list populated
- `extract.png`  — AWB quick extract mid-run with progress bar
- `inject.png`   — one waveform queued for replacement with the pending panel visible
- `convert.png`  — batch WAV↔HCA with a couple files queued
- `about.png`    — the About dialog for the hero image
